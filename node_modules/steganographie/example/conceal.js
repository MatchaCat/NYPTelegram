/*************************************
* DEPENDENCIES
*************************************/

/**
 * Note:
 * Don't forgot to install path
 */
var steganographie = require('../index');
var path = require('path');

/*************************************
* CONCEAL
*************************************/

let parameters = {
  input: path.resolve(__dirname, 'data/img_1.png'),
  output: path.resolve(__dirname, 'data/img_conceal.png'),
  method: 'simple',
  text: 'I\'m a super secret message, is the NSA looking for me?',
}

steganographie.conceal(parameters, (err, res) => {
  // Retrieve result
  if(err) console.log('Hmm, there is an error with conceal: ');
  console.log(res);
});