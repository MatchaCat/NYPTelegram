/*************************************
* DEPENDENCIES
*************************************/

/**
 * Note:
 * Don't forgot to install path
 */
var steganographie = require('../index');
var path = require('path');

/*************************************
* REVEAL
*************************************/

let parameters = {
  input: path.resolve(__dirname, 'data/img_1.png'),
  method: 'simple'
}

steganographie.reveal(parameters, (err, res) => {
  // Retrieve result
  if(err) console.log('Hmm, there is an error with reveal: ');
  console.log(res);
});